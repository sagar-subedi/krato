package main

func main() {
	// Coordinator routing logic has been fully integrated into the `node` entrypoint directly.
	// This binary stub is maintained for future decoupling if cluster scaling strictly demands API load balancers natively separated from storage daemons.
}
